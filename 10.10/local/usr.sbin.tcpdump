# Site-specific additions and overrides for usr.sbin.tcpdump.
# For more details, please see /etc/apparmor.d/local/README.

  # tcpdump -z is commonly used with gzip/bzip2 (LP: #722856)
  /bin/gzip	ix,
  /bin/bzip2	ix,
